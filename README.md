# homebrew-tap

Homebrew formulae for [ruth-cli](https://github.com/zhongdai/ruth-cli) — an agent-friendly TOTP authenticator CLI.

## Install

```bash
brew tap zhongdai/tap
brew install ruth-cli --HEAD
```

## Update

```bash
brew upgrade ruth-cli --fetch-HEAD
```
